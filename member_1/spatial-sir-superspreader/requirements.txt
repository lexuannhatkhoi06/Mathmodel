# Spatial SIR with superspreaders — runtime dependencies
# Install with:  pip install -r requirements.txt
# Requires Python >= 3.9

numpy>=2.0        # np.trapezoid (used in member1_percolation.py) needs numpy 2.0+
scipy>=1.9        # scipy.spatial.cKDTree neighbour search
matplotlib>=3.5   # figure rendering
